# Invisible-Cloak
This project creates an “invisibility cloak” effect using computer vision. By detecting a specific color range in the video frame (for example, red), it replaces that region with the background captured earlier — making the selected color appear invisible in real-time.
